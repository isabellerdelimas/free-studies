{
  "name": "free-studies",
  "version": "1.0.0",
  "description": "A small static study quiz for science review.",
  "private": true,
  "scripts": {
    "build": "npm test && node scripts/build-static.js",
    "start": "node scripts/local-server.js",
    "test": "node test/multiplayer.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
